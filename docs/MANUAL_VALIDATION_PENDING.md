# SlickClip Manual Validation Pending

This is the short human-validation checklist for provisional automated checkpoints. A listed stage is implemented and automatically validated, but is not considered manually verified.

## Stage 19 — Professional UI/UX Polish

- Checkpoint: `c62c81f` (`Stage 19 - professional UI and UX polish [manual validation pending]`)
- Why human validation is required: WebView2 appearance, Windows scaling, real media interaction, fullscreen, focus, responsive layout, and reduced-motion behavior cannot be proven by unit/build checks.
- Shortest practical test: Launch SlickClip; inspect Sidebar, Replay, Clips, ClipPlayer, Editor, and Settings at narrow and maximized widths. Exercise Replay start/stop and Save Replay; Clips search/grid/More menu/Copy Clip; ClipPlayer controls; Editor custom transport, EDL seek after deleting a segment, mixer, fullscreen, and export; Hotkey Test; then repeat interaction checks with Windows animation effects disabled.
- Expected pass behavior: Graphite/purple presentation remains readable and uncluttered; diagnostics are collapsed; card menus remain inside the window and keyboard-operable; Editor seeking stays in edited time; no controls overlap or disappear; capture, save, playback, edit, mixer, export, preferences, and hotkeys behave as before.
- Specific risks to inspect: collection-menu focus and viewport placement, Compact card density, Editor fullscreen/custom seek behavior, seeking across cuts, Windows display scaling, warnings remaining visible, and decorative motion under reduced-motion settings.

## Stage 20 — Replay Roulette

- Checkpoint: `6b40cc6` (`Stage 20 - add Replay Roulette [manual validation pending]`)
- Why human validation is required: the native WebView layout, thumbnail generation, modal ClipPlayer integration, real Library mutations, Windows scaling, and reduced-motion presentation cannot be proven by selection-unit tests or a production build.
- Shortest practical test: Prepare at least seven clips with a mix of favorites, collection membership, play counts, and last-watched times. Open Replay Roulette; verify All Collections and Favorites-only counts and empty states; choose six replays and confirm a pick does not immediately repeat while alternatives exist; open a result, play at least three meaningful seconds, seek, change volume, copy it, and close the player; spin again; then repeat the layout check at narrow and maximized widths with Windows animation effects disabled.
- Expected pass behavior: filters constrain the eligible pool, recent roulette picks stay out of rotation until necessary, less-played/less-recent clips are favored over time, every chosen thumbnail opens the existing ClipPlayer, watch metadata still updates, Copy Clip shows feedback, empty/error/loading states remain clear, and the restrained reveal disappears under reduced motion.
- Specific risks to inspect: probabilistic weighting over repeated runs, one-clip and fully filtered pools, deleted collections, asynchronous thumbnail readiness, player focus/escape handling, long clip names, narrow widths, high display scaling, and motion settings.

## Stage 21 — Animated Launch Experience

- Checkpoint: `f5c14f2` (`Stage 21 - add animated launch experience [manual validation pending]`)
- Why human validation is required: only a real packaged/native launch can verify whether the splash paints before initialization work, stays centered, avoids taskbar/focus artifacts, hands focus to the main WebView cleanly, and respects the user’s Windows motion preference.
- Shortest practical test: Cold-launch SlickClip normally and with Windows animation effects disabled. In each mode confirm the black borderless splash appears centered with SlickClip branding, the main window is not visible behind it, and exactly one focused main window replaces it without a white flash, stranded splash, taskbar duplicate, or input delay. Repeat once after an unclean process termination and once on a high-DPI display setting.
- Expected pass behavior: normal mode shows restrained purple hex/progress motion during real initialization; reduced-motion mode is static and hands off faster; startup state initializes once; the splash closes; the main window becomes visible and focused at its normal size; and the eight-second safety reveal is never noticeable during a healthy launch.
- Specific risks to inspect: first-frame white flash, splash loading too late to be useful, high-DPI centering, focus loss, taskbar duplication, WebView asset-load failure, slow initialization beyond eight seconds, reduced-motion detection, and closing the app during splash display.

## Stage 22 — Tray, Background, Startup, and Save Overlay

- Checkpoint: `db7328c` (`Stage 22 - add tray and background lifecycle [manual validation pending]`)
- Why human validation is required: Windows tray menus, HKCU startup registration, native close/minimize events, background capture continuity, multi-monitor placement, focus behavior, and transparent always-on-top overlays require a real interactive Windows session.
- Shortest practical test: Launch SlickClip and confirm one tray icon reports Replay Buffer stopped. Start a replay buffer, wait for a finalized segment, and confirm the tray reports the selected target and enables Save Replay. With “Close or minimize to tray” on, minimize and then close the main window; verify SlickClip leaves the taskbar, capture counters continue advancing, tray Save Replay and the global hotkey each produce a valid Library clip, and the lower-right Replay Saved overlay never steals focus from another app. Open SlickClip from the tray, test the overlay-off preference, enable Windows startup, sign out/in (or run the registered command), confirm background launch creates only the tray presence, then disable startup and verify the registration is removed. Finally turn close-to-tray off, close the window, and confirm the process exits; repeat Quit from the tray while capture is active.
- Expected pass behavior: tray status/save availability follows the real replay/save managers; hide/restore preserves the active buffer and UI state; only explicit Quit or closing with background mode disabled exits and cleans capture; startup opens quietly with `--background`; preferences survive restart; successful saves show one approximately 3.2-second noninteractive overlay on the correct monitor without changing foreground focus; disabled overlays stay hidden.
- Specific risks to inspect: duplicate or missing tray icons, stale status, Save Replay enable timing, minimize/restore loops, unsaved Editor state while hiding, fullscreen capture while hidden, registry paths containing spaces, startup after app relocation/update, taskbar flashes, multi-monitor work areas and scaling, overlay focus theft, repeated saves extending overlay duration, shutdown during an active save, and preference-write/registry rollback errors.

## Stage 23 — Game Detection and Auto-Arm

- Checkpoint: `4c8e3ec` (`Stage 23 - add safe game detection and auto-arm [manual validation pending]`)
- Why human validation is required: launcher/process behavior, Windows capture enumeration, borderless/fullscreen games, process-loopback audio, anti-cheat-protected windows, and real false-positive/false-negative rates cannot be represented faithfully by unit fixtures.
- Shortest practical test: With detection off, open a game, launcher, browser, Discord, media player, and productivity app and confirm nothing starts. Enable detection and verify only plausible large windows appear as unapproved suggestions. Approve one game process, enable auto-arm, and confirm capture does not start until exactly one approved window is live; wait for `running`, confirm the non-focus-stealing Ready notification and tray target, save a replay, and verify video plus the approved process’s game-audio track. Open a second approved game window and confirm an idle buffer does not auto-arm while selection is ambiguous. Exclude the active process and confirm a buffer started by auto-arm stops; then manually start an unrelated buffer and confirm detection never stops or replaces it. Close/relaunch the game, test windowed/borderless/fullscreen modes and a launcher transition, restart SlickClip to verify rules, and repeat from background startup.
- Expected pass behavior: heuristics only suggest; only an explicit process approval can auto-arm; exclusions override approvals; small, launcher, SlickClip, browser, Discord, OBS, and productivity windows are not suggested by default; ambiguous approved windows block auto-arm; the target is re-resolved before start; game process audio follows the detected PID; Ready appears only after replay state is running; auto-armed capture stops when its approved window closes or loses approval; manually started capture remains under manual control.
- Specific risks to inspect: launchers that reuse the game executable, games with multiple top-level windows, executable renames, UWP titles, anti-cheat/elevated processes, transient splash windows, fullscreen enumeration, PID changes, process audio availability, 15-second error retry behavior, manual Stop while auto-arm remains enabled, false positives from unknown productivity apps, focus changes, and detection shutdown during app exit.

## Stage 24 — Storage Safety

- Checkpoint: `bdfc167` (`Stage 24 - add storage safety controls [manual validation pending]`)
- Why human validation is required: destructive behavior, actual Windows filesystem semantics, live SQLite migration, cache removal, long clip lists, and the clarity of a permanent-deletion preview require a real application session with disposable media.
- Shortest practical test: Back up the SlickClip app-data and Clips folders, then use a disposable Windows user/profile or replace them with a disposable test set containing at least five valid MP4 clips with distinct creation times and sizes. Favorite one old clip without protecting it; protect a different old clip; set a quota that requires two deletions; click `Save Quota & Preview`; verify the preview lists the oldest unprotected clips in order, includes the favorited-but-unprotected clip when its turn arrives, excludes the protected clip, and shows expected reclaimed/remaining sizes. Cancel once and verify nothing changed. Preview again, confirm deletion, then verify only the listed MP4s and their owned caches/database rows disappeared. Restart SlickClip and verify quota/protection persistence. Finally change protection or add a clip after preview and verify the stale plan refuses to run until re-previewed.
- Expected pass behavior: no deletion occurs during preview or after Cancel; Favorite and Protected remain independent; only direct-child regular MP4 files resolved from trusted Library IDs can be removed; cleanup is oldest-unprotected-first and stops when the quota plan is satisfied; changed plans are rejected; protected and unlisted files remain byte-for-byte present; successful cleanup refreshes totals and removes associated owned cache artifacts.
- Specific risks to inspect: upgraded Stage 18 database migration, one/no clip, all clips protected, protected bytes alone exceeding quota, very long names/lists, quota bounds (1 GB–10 TB), concurrent save/pin/manual-delete activity, missing or locked files, symlinks/reparse points, files outside `Videos\JustIn Replay\Clips`, partial cleanup after an I/O failure, and Settings layout at narrow/high-DPI sizes.

## Stage 25 — Final SlickClip Migration and Distribution

- Checkpoint: `cc79345` (`Stage 25 - finalize SlickClip migration and installer [manual validation pending]`)
- Artifact: `src-tauri\target\release\bundle\nsis\SlickClip_1.0.0_x64-setup.exe`, 87,131,060 bytes, SHA-256 `B49C34D313E240DE305D44D978C5846480BDAE2A7E67043B8AC8584AC0755622`. This local candidate is intentionally unsigned; Stage 26 owns signing and the release feed.
- Why human validation is required: a real disposable upgrade and a clean Windows profile are required to prove installer/WebView bootstrap behavior, taskbar/tray branding, first-launch filesystem migration, capture permissions, bundled FFmpeg execution, end-to-end media workflows, and uninstall preservation. GPL distribution/source-notice adequacy and release signing also require explicit release review.
- Shortest practical test: Back up all real SlickClip/legacy data and use disposable Windows profiles or VMs. On an upgrade profile, create representative legacy clips plus favorites, Protected state, collections, play counts, preferences, thumbnails/previews, and cached editor audio; record hashes, install the candidate, launch once, and confirm every item moved to the SlickClip roots with bytes/metadata intact and no legacy collision overwritten. Relaunch to prove idempotence. Separately create a deliberate same-name destination collision and confirm startup refuses migration without changing either copy. On a clean profile, install as a standard user; confirm SlickClip 1.0.0 branding in installer, Start menu, taskbar, tray, executable metadata, splash, and UI; verify no separate FFmpeg is required; start capture, save a replay, play/copy it, edit it, export H.264/AAC, restart, and repeat playback. Uninstall, decline application-data removal if offered, and confirm permanent clips remain under `Videos\SlickClip\Clips`; reinstall and confirm retained data is usable.
- Expected pass behavior: install/launch works without developer tools or PATH FFmpeg; migration is one-time/retry-safe and never overwrites conflicts or follows reparse points; clip bytes and all Library/preferences/cache metadata survive; newly saved/exported files use SlickClip naming; capture/audio/player/editor/export behavior matches earlier stages; uninstall removes application binaries but never permanent clips; all branding is SlickClip except intentional legacy compatibility diagnostics; Windows warnings match the candidate’s unsigned status.
- Specific risks to inspect: non-ASCII/long user paths, current-only installs with no Clips directory, legacy/current trees both present, locked database/cache files, interrupted migration and retry, junctions/symlinks, missing legacy media referenced by SQLite, high-DPI installer/splash/UI, WebView bootstrap offline behavior, bundled `ffmpeg.exe`/`ffprobe.exe` discovery, antivirus quarantine, tray/startup registration after path change, uninstall data checkbox behavior, GPL license/source notice visibility, unsigned SmartScreen warnings, and upgrade from the last legacy build.

## Stage 26 — Updater and Release Candidate

- Checkpoint: `3322c46` (`Stage 26 - add signed updater release workflow [credentials and manual validation pending]`)
- External blocker: production Tauri updater key/public key custody, an approved Windows code-signing identity and usable `SLICKCLIP_WINDOWS_SIGN_COMMAND`, HTTPS manifest/artifact URLs, and publishing access are not available in this workspace. No signed candidate or feed was fabricated; v1.0.0 is not release-approved.
- Why human validation is required: Authenticode reputation/trust, updater signature verification, HTTPS hosting, passive NSIS replacement/restart, live manager shutdown, data preservation, interrupted/failing updates, clean-PC compatibility, and the full release gate require signed sequential builds and real Windows VMs.
- Shortest practical test: Provision the approved secrets/URLs exactly as documented in `docs/RELEASE_PROCESS.md`. On a clean release-machine checkout run `npm run bundle:release`; independently verify valid Authenticode, updater `.sig`, `latest.json`, version metadata, and recorded hashes. Install signed version A on clean and upgrade VMs, create disposable clips/settings/collections/protection/watch metadata, and verify the endpoint's no-update response. Publish a signed higher SemVer version B artifact first and manifest last; Check for Updates, inspect version/notes, choose Update & Restart while the Replay Buffer is running, and confirm one passive install/relaunch into version B with clips/settings intact and capture/save/playback/edit/export still working. Repeat with an unreachable endpoint, malformed manifest, wrong signature, interrupted download, and a version that changes between check and install; confirm version A remains installed and usable. Test uninstall preservation and recovery by publishing a higher fixed SemVer rather than weakening downgrade/signature checks, then complete every item in `RELEASE_GATE.md`.
- Expected pass behavior: only HTTPS manifests and artifacts signed by the embedded updater key are accepted; only one update operation runs; a changed version requires a new check; failures never install unverified bytes or remove user data; successful Update & Restart performs orderly manager cleanup, passive per-user replacement, one relaunch, preserved data, and truthful version/branding; release artifacts show valid intended-publisher Authenticode; feed publication never exposes a missing artifact.
- Specific risks to inspect: lost/mismatched updater key, certificate expiration/timestamping, SmartScreen reputation, proxy/TLS failures, antivirus locks, partial download, signature mismatch, stale CDN manifest, version/channel mistakes, active replay/save/export shutdown timing, installer process race, startup/tray registration after replacement, launch arguments, update loops, feed rollback pressure, non-admin user install, offline behavior, and every still-pending Stage 19–25 manual gate.
